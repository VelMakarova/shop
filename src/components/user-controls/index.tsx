import React from 'react';
import { StyledControls } from './user-controls.styled';

export const UserControls: React.FC = () => {
  return <StyledControls className="user-controls">control</StyledControls>;
};
